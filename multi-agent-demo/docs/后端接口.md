明白了，你希望工具 API **更贴近文章写作场景**，而不仅仅是客户/订单/商品查询。我帮你重新设计一个 **文章相关的工具 API Mock 服务**，让多智能体系统可以调用这些工具来辅助写作、审稿、编辑和发布。

---

# 📁 项目结构

```
article-tools-backend/
 ├─ index.js       # 启动服务
 └─ mockData.js    # Mock 数据
```

---

# 1️⃣ mockData.js

```js
export const templates = [
  { id: "TPL-001", name: "新闻报道模板", content: "标题：...\n正文：..." },
  { id: "TPL-002", name: "技术文章模板", content: "摘要：...\n正文：..." },
  {
    id: "TPL-003",
    name: "博客模板",
    content: "开头：...\n正文：...\n结尾：...",
  },
];

export const glossary = [
  { term: "AI", definition: "人工智能" },
  { term: "RAG", definition: "检索增强生成" },
  { term: "LangChain", definition: "一个构建智能体的框架" },
];

export const references = [
  {
    id: "REF-001",
    title: "深度学习综述",
    url: "https://example.com/dl-review",
  },
  {
    id: "REF-002",
    title: "自然语言处理进展",
    url: "https://example.com/nlp-progress",
  },
];
```

---

# 2️⃣ index.js

```js
import express from "express";
import bodyParser from "body-parser";
import { templates, glossary, references } from "./mockData.js";

const app = express();
const PORT = 3001;

app.use(bodyParser.json());

// 健康检查
app.get("/health", (req, res) => {
  res.status(200).json({
    status: "OK",
    service: "article-tools-backend",
    mockMode: true,
    timestamp: new Date().toISOString(),
  });
});

// 获取文章模板
app.post("/api/template/get", (req, res) => {
  const { templateId } = req.body;
  if (!templateId) return res.status(400).json({ error: "缺少 templateId" });

  const tpl = templates.find((t) => t.id === templateId);
  if (!tpl) return res.status(404).json({ error: "模板未找到" });

  res.json(tpl);
});

// 查询术语解释
app.post("/api/glossary/term", (req, res) => {
  const { term } = req.body;
  if (!term) return res.status(400).json({ error: "缺少 term 字段" });

  const entry = glossary.find(
    (g) => g.term.toLowerCase() === term.toLowerCase()
  );
  if (!entry) return res.status(404).json({ error: "术语未找到" });

  res.json(entry);
});

// 查询参考文献
app.post("/api/reference/get", (req, res) => {
  const { refId } = req.body;
  if (!refId) return res.status(400).json({ error: "缺少 refId" });

  const ref = references.find((r) => r.id === refId);
  if (!ref) return res.status(404).json({ error: "参考文献未找到" });

  res.json(ref);
});

// 自动生成文章段落（简单 Mock）
app.post("/api/paragraph/generate", (req, res) => {
  const { topic, style } = req.body;
  if (!topic) return res.status(400).json({ error: "缺少 topic" });

  const paragraph = `这是关于“${topic}”的一段${
    style || "普通"
  }风格的示例文本。`;
  res.json({ paragraph });
});

app.listen(PORT, () => {
  console.log(
    `Article Tools Backend Mock API running at http://localhost:${PORT}`
  );
});
```

---

# 3️⃣ 使用示例

```bash
# 健康检查
curl http://localhost:3001/health

# 获取模板
curl -X POST http://localhost:3001/api/template/get \
  -H "Content-Type: application/json" \
  -d '{"templateId":"TPL-002"}'

# 查询术语
curl -X POST http://localhost:3001/api/glossary/term \
  -H "Content-Type: application/json" \
  -d '{"term":"AI"}'

# 查询参考文献
curl -X POST http://localhost:3001/api/reference/get \
  -H "Content-Type: application/json" \
  -d '{"refId":"REF-001"}'

# 自动生成段落
curl -X POST http://localhost:3001/api/paragraph/generate \
  -H "Content-Type: application/json" \
  -d '{"topic":"智能体应用","style":"正式"}'
```

---

# 4️⃣ 特点

1. **贴近写作场景**：提供模板、术语、参考文献、段落生成
2. **可扩展**：未来可加更多写作辅助功能，比如自动摘要、标题生成、段落优化
3. **Mock 数据**：无数据库依赖，开箱即用
4. **易于集成到五 Agent 系统**：Agent 可以自动调用接口，例如 Writer Agent 自动生成段落，Reviewer Agent 自动检查术语

---

我可以帮你 **把这个 Article Tools Backend 完全集成到现有五 Agent Demo 项目**，让 Writer/Reviewer/Author/Editor/Publisher 都能自动调用这些写作工具 API，形成真正的多智能体文章撰写完整流程。

你希望我帮你集成吗？
